CSS margin is used to create space outside the element’s border. It controls how much space exists between elements.

🔹 1. margin
Shorthand property to set margins on all four sides: top, right, bottom, and left.
Syntax:
margin: 10px; → all sides
margin: 10px 20px; → top & bottom: 10px, left & right: 20px
margin: 10px 15px 20px 25px; → top, right, bottom, left (clockwise)

🔹 2. margin-top / margin-right / margin-bottom / margin-left
Set margin on a specific side.

🔹 3. margin: auto
Used to center block-level elements horizontally (especially with fixed width).
Example: margin: 0 auto;

🔹 4. Negative Margins
You can use negative values to pull elements closer together.
Example: margin-top: -10px;

🔹 5. Initial and Inherit
margin: initial; → resets to browser default.
margin: inherit; → inherits margin from the parent element.

